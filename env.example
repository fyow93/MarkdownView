# MarkdownView 配置文件

# 项目根路径 - 指向包含Markdown文件的目录
# 如果不设置，默认使用 ~/project-wiki
MARKDOWN_PROJECT_ROOT=/path/to/your/markdown/project

# 服务器配置
PORT=3000
HOST=localhost

# 文件监控配置
POLL_INTERVAL=3000

# Node.js 环境
NODE_ENV=development 