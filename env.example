# MarkdownView 可选配置文件
# 这些都是可选配置，如果不设置将使用默认值

# 服务器配置（可选）
# PORT=3000
# HOST=localhost

# 文件监控配置（可选）
# POLL_INTERVAL=3000

# Node.js 环境（可选）
# NODE_ENV=development

# 注意：项目根目录不再通过环境变量配置
# 请在应用界面中点击 "Change Directory" 按钮选择您的项目目录 